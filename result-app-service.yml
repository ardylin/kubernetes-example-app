apiVersion: v1
kind: Service
metadata:
  name: result-service
  labels:
    name: result-service
    app: demo-result-app
spec:
  type: NodePort
  ports:
  - port: 
    targetPort: 88
  selector:
     name: 
     app:
